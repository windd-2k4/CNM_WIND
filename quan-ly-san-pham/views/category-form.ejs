<!DOCTYPE html>
<html>
<head>
    <title><%= category ? 'Sửa' : 'Thêm' %> Danh mục</title>
    <style>
        body { font-family: sans-serif; padding: 20px; max-width: 500px; margin: 0 auto; }
        input, textarea { width: 100%; margin-bottom: 15px; padding: 8px; box-sizing: border-box; }
        button { padding: 10px 20px; background: #007bff; color: white; border: none; cursor: pointer; }
    </style>
</head>
<body>
    <h2><%= category ? 'Chỉnh sửa' : 'Thêm mới' %> Danh mục</h2>
    
    <% const actionUrl = category ? '/categories/edit/' + category.categoryId : '/categories/add'; %>
    
    <form action="<%= actionUrl %>" method="POST">
        <label>Tên danh mục:</label>
        <input type="text" name="name" value="<%= category ? category.name : '' %>" required>
        
        <label>Mô tả:</label>
        <textarea name="description" rows="4"><%= category ? category.description : '' %></textarea>
        
        <button type="submit">Lưu lại</button>
        <a href="/categories" style="margin-left:10px; text-decoration:none; color:#555;">Hủy</a>
    </form>
</body>
</html>